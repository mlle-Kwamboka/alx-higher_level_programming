#!/usr/bin/python3
def print_reversed_list_integer(my_list=[]):
    i = len(my_list)
    while i >= 0:
        my_list[i] = my_list[-1]
        my_list += 1
    j -= 1
    print("{}".format(my_list[i]))
