#!/usr/bin/python3
def uppercase(str):
    for i in str:
        if ord(i) >= ord('a') and ord(i) <= ord('z'):
            print("{}".format(chr(i=range(ord('A'), ord('Z') + 1))))
        else:
            print("{}".format(chr(i)))

