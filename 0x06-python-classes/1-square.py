#!/usr/bin/python3
class Square:
    """Classs square is used to define a square"""

    #instantiating private attribute, size
    def __int__(self):
        self.__size = size
y = Square()
print(y)
